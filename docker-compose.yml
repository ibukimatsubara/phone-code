services:
  phone-code:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - /tmp:/tmp
      - /var/run/tailscale/tailscaled.sock:/var/run/tailscale/tailscaled.sock
    restart: unless-stopped
